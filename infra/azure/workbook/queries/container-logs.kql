ContainerLogV2
| where TimeGenerated {TimeRange}
| where PodNamespace == "{Namespace}"
| where ContainerName in ({Container})
| project TimeGenerated, Container = ContainerName, LogEntry = tostring(LogMessage)
| order by TimeGenerated desc
