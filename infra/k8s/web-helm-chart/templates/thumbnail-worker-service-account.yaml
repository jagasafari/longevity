apiVersion: v1
kind: ServiceAccount
metadata:
  name: thumbnail-worker-sa
  namespace: {{ .Values.namespace }}
  annotations:
    azure.workload.identity/client-id: {{ .Values.worker.workloadIdentityClientId }}
