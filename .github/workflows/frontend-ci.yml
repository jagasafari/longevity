name: Frontend CI

on:
  push:
    paths:
      - "longevity-frontend/**"
      - ".github/workflows/frontend-ci.yml"
  pull_request:
    paths:
      - "longevity-frontend/**"
  schedule:
    - cron: "0 0 * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore
        run: dotnet restore longevity-frontend/longevity-frontend.csproj

      - name: Build
        run: dotnet build longevity-frontend/longevity-frontend.csproj --no-restore -c Release

      - name: Publish
        run: dotnet publish longevity-frontend/longevity-frontend.csproj --no-build -c Release -o publish

      - name: Verify output
        run: test -f publish/wwwroot/index.html
